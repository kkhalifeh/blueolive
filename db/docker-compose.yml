version: '3.8'
services:
  db:
    image: postgis/postgis:15-3.4
    environment:
      - POSTGRES_USER=blueolive
      - POSTGRES_PASSWORD=securepassword
      - POSTGRES_DB=blueolive_db
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
